# Check that x is a data.frame

Check that x is a data.frame

## Usage

``` r
.check_df(x, arg = "data")
```

## Arguments

- x:

  Object to check.

- arg:

  Name of the argument (for error message).
